import { Keypair } from "@solana/web3.js";
import { loadHomeKeypair } from "./utils";
import { base16, base32, base64, base58 } from "@scure/base";

const main = async () => {
  // const kp: Keypair = await loadHomeKeypair("moodi-shdw-keypair");
  // console.log(base58.encode(Buffer.from(kp.secretKey)));
};

main();
